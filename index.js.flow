interface WindowScrollPosition {
  x: number;
  y: number;
}

interface WindowScrollPositionOptions {
  passive?: boolean;
  throttle?: number;
}

declare export default function useWindowScrollPosition(
  options?: WindowScrollPositionOptions
): WindowScrollPosition
